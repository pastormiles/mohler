# =============================================================================
# YouTube AI Search Server - Dependencies
# =============================================================================
# Use this file when deploying ONLY the server to production
# (The main requirements.txt includes everything for local development)
# =============================================================================

flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0
openai>=1.0.0
pinecone>=5.0.0
python-dotenv>=1.0.0

# For GCP deployment
google-cloud-firestore>=2.0.0

# For GitHub API calls
requests>=2.31.0
